[package]
name = "myfind"
version = "0.1.1"
authors = ["Will Shuttleworth <wps20@pitt.edu>"]
license = "MIT"
description = "Like the `find` util, but useless"
readme = "README.md"
keywords = ["cli", "find", "practice"]
categories = ["command-line-utilities"]

[dependencies]
clap = { version = "4.0.32", features = ["derive"] }
